

export interface ChoiceItem {
  value?: string;
  label: string;
}

// import * as React from "react";

// import filterData from "../utils/filterData.json";
// import HierarchyFilterGroup, { RenderTree } from "./HierarchyFilterGroup";

// var jsonValues = filterData.filterFamilies[0].primaryView.groups[0].filters[0]
//   .value as any;

//   var renderValues = jsonValues.items as RenderTree[]; 


// export default function CategoryFilterGroup() {
//   return (
//     <HierarchyFilterGroup
//       onClick={() => {}}
//       heading="Välj ett ämne"
//       items={renderValues}
//     />
//   );
// }
